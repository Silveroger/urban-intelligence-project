# Coding Rule

- Use existing project conventions.
- Prefer simple readable code over abstractions without current value.
- Avoid giant files/functions.
- Avoid duplicate helpers.
- Keep side effects isolated.
- Add types at boundaries.
- Handle loading, empty, error, and null cases where relevant.
- Do not hardcode environment-specific values.
- Do not add a library when existing dependencies solve the problem.
